import { component$ } from "@builder.io/qwik";

import styles from "./polygon.module.css"; // eslint-disable-line @typescript-eslint/no-unused-vars

export const Polygon = component$(() => {
  return <></>;
});
